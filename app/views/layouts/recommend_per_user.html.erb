
Hello, <%= @user.first_name %>
This month the most popular items based on your choice were
<% @recommendations[:recs].each do |pos| %>
  <% item = Upload.find(pos[:item_id]) %>
  <%= item.name %> for <%= item.date %>
  With score <%= pos[:score] %>
<% end %>
The best subscription we can propose is:
 <% subs = Subscription.select("uploads_ratings -> 'r'", @recommendations[:future_rating][0].round) %>
 <% subs.each do |sub| %>
 <%= sub&.id %>
  With rating <%= @recommendations[:future_rating][0] %>
 <% end %>
